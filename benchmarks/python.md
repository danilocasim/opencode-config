# Python Benchmarks

## Build a typed tool module with pytest

Prompt:

"Create a small Python module that normalizes emails. Use strict typing, explicit errors, and pytest tests. Provide a minimal pyproject.toml toolchain using uv + ruff + pyright."

Expected loads:

- `skills/python/SKILL.md`
- `skills/testing/python.md`

Expected traits:

- Type hints everywhere.
- Deterministic behavior (no hidden globals).
- Tests cover error path.
